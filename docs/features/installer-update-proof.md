# Installer and updater proof inventory

This hand-written inventory separates evidence that is often incorrectly combined. A source test, packaged file, successful installation, running surface, capture, and published release each prove a different boundary. Version `0.1.1` remains unreleased until the remote rows are verified.

| Surface or contract | Source evidence | Package evidence | Installation evidence | Installed runtime evidence | Capture evidence | Remote evidence | Current verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Squirrel.Windows-only installer | 1 focused installer contract test recorded | `Setup.exe`, `RELEASES`, and full `.nupkg` generated by the final root candidate build | Silent headless installation succeeded | Installed pre-release application launched | Pre-release installed capture exists | No `0.1.1` release or remote asset verification | Local verified, remote pending |
| Unsigned policy | Signing controls disabled in source contract | Authenticode reported `NotSigned` for setup | Installer remained intentionally unsigned | Installed application must not claim signature authenticity | Warning copy is documented; current operating-system warning capture is not claimed | Published release warning not verified | Local package verified, remote pending |
| Packaged catalog | Data and addition validators cover the assembled catalog | Packaged `resources/app.asar/catalog.json` inspected at exactly `2919` records | Installed payload came from the inspected candidate | Installed pre-release surface rendered `2919` | Installed pre-release capture showed `2919` | Published asset not verified | Local verified, remote pending |
| Updater engine | 24 focused engine tests recorded before final core merge, plus lifecycle/feed coverage | Updater code is present in the candidate package | Installed candidate includes updater components | Update request reached the failure surface and reported HTTP 404 | Installed pre-release capture records the honest 404 | `update.json` is not published | Failure path verified, success path pending |
| Updater interface | 10 focused UI tests recorded | UI module included in candidate payload | Installed surface available | Exact unavailable/error facts shown without false success | Pre-release 404 capture exists | Published ready/current states unavailable | Failure path verified, remote pending |
| Feed integrity and rollback | Source coverage records malformed feed, invalid hash, corrupt asset, offline, cancellation, and rollback states | No published feed package exists to inspect | No update installation can run until metadata exists | Offline/failure behavior is documented; real published-feed cycle unrun | No current-feed or ready-to-restart capture | Metadata, asset hashes, downloadability, staging, restart, rollback, and current-version result unverified | Pending |

## Completion rules

- A local source or package result never marks a remote release row complete.
- A successful installation never proves the updater can download or apply a later release.
- The observed HTTP 404 is correct pre-release behavior because `update.json` is absent. It is not a successful update check.
- Remote completion requires a unique non-draft `0.1.1` release, immutable target commit, downloadable `Setup.exe`, `RELEASES`, full `.nupkg`, hashes, unsigned warning, published metadata, and an installed update cycle through ready-to-restart and current.
- Capture completion requires the real installed artifact at the exact verified release commit. A source preview or pre-release error capture cannot substitute for the published success states.

Suggested articles: [Windows Electron companion](electron.md), [Automatic updates](automatic-update.md), [Documentation index](../README.md).
